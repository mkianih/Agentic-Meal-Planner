# Copy this file to .env and fill in your key.
# Get a free Gemini API key at https://aistudio.google.com/app/api-keys
#
# This project talks to Gemini through its OpenAI-compatible endpoint, so the
# code is ordinary OpenAI SDK code - only this key and the base_url differ.
GOOGLE_API_KEY=your-key-here
